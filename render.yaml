services:
  - type: web
    name: poetic-brushstrokes
    env: node
    buildCommand: npm install && npm run build
    startCommand: node server.cjs
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: VOLCANO_API_KEY
        sync: false
      - key: PORT
        value: 10000
